# DNS-Benchmark
A DNS Benchmarking tool written in C, it will start as just a CLI and then I will expand it to a full GUI.

This is going to be a fully human written program designed to throw me into the fire with C.
